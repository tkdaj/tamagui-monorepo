#!/usr/bin/env node
import { writeFileSync } from "fs";
import { resolve, dirname } from "path";
import { fileURLToPath } from "url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const outputPath = resolve(__dirname, "../../../lib/config/src/generated/web-routes.d.ts");

// This is a placeholder implementation
// In a real scenario, you would parse the generated routeTree.gen.ts
// and extract type information from it

const content = `// This file is generated by apps/web/scripts/extract-web-route-types.mjs
// DO NOT EDIT MANUALLY

export type WebRoutePath = 
  | "/"
  | "/login"
  | "/auth/callback"
  | "/demo"
  | "/demo/file-based"
  | "/demo/code-based"
  | string;
`;

writeFileSync(outputPath, content, "utf-8");
console.log("✓ Generated web route types at", outputPath);
